apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: cortex
  name: "release-name-cortex-job-io"
  labels:
    app: cortex
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi